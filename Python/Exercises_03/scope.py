# This code divides two ints by taking them as values within the function.
def divisible(numerator: int, denominator: int)->bool:
    # checks if the reaminder of the denominator is equal to 0
    return numerator % denominator == 0
# Calls the function with two values 30 and 4
print(divisible(30,4))

# This code would return true if the return value is 0

# This code divides two ints by taking them as values within the function.
def divisible(numerator: int, denominator: int)->bool:
    # checks if the reaminder of the denominator is equal to 0
    return numerator % denominator == 0
# Calls the function with two values 30 and 4
print(divisible(30,5))

# The above code returns true